winsync.bat
script to synchronize folders between the cluster and a windows machine


== installation ==

1. generate a public/private key pair under cluster account

2. copy the private key from the cluster to the winsync folder

3. convert the key to usable format using puttygen.exe

4. edit winsync.bat and enter
   -cluster username
   -remote path from which to copy data
   -local path to which data is copied



== usage ==

execute winsync.bat
